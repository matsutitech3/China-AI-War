name: Build the book

on:
  push:
    branches: [main]
    paths:
      - '**.tex'
      - 'Makefile'
      - '.github/workflows/build.yml'
  pull_request:
    paths:
      - '**.tex'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install TeX Live
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-latex-base texlive-latex-recommended texlive-latex-extra \
            texlive-fonts-recommended texlive-fonts-extra texlive-pictures \
            texlive-science make

      - name: Compile (three passes)
        run: make

      - name: Verify cross-references resolve
        run: make check

      - name: Report page count
        run: |
          sudo apt-get install -y poppler-utils
          pdfinfo main.pdf | grep Pages

      - name: Upload the built PDF
        uses: actions/upload-artifact@v4
        with:
          name: book-pdf
          path: How-China-Will-Win-the-AI-War-*.pdf
          if-no-files-found: error

      # On a tagged release, attach the PDF so every version is downloadable.
      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: How-China-Will-Win-the-AI-War-*.pdf
